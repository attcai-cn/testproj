<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbt.mapper.UserMapper">
    <update id="updateById">
        update `user`
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="delById">
        delete from `user` where id = #{id}
    </delete>
    <select id="selectAll" resultType="com.example.springbt.entity.User">
        select * from `user`
                 <where>
                     <if test="username != null and username != ''">
                         username like concat('%',#{username},'%')
                     </if>
                     <if test="name != null and name != ''">
                         and name like concat('%',#{name},'%')
                     </if>
                     <if test="ids != null and ids != ''">
                         and id in
                         <foreach collection="idsArr" item="id" separator="," open="(" close=")">
                             #{id}
                         </foreach>
                     </if>
                 </where>
                 order by id desc
    </select>

    <select id="insertInto">
        insert into `user` (username, password, name, email, phone)
        values(#{username}, #{password}, #{name}, #{email}, #{phone})
    </select>
    <select id="selectByUsername" resultType="com.example.springbt.entity.User">
        select * from `user` where username = #{username}
    </select>
    <select id="selectById" resultType="com.example.springbt.entity.User">
        select * from `user` where id = #{id}
    </select>

</mapper>